# Three-zone image prompt (saved)

Frozen on 2026-09-23, before the cohesive-scene revision.

These two files are the image generator as it was producing frames: a top zone, a dark middle band, and a lower zone. `scene-writer-prompt.txt` is the scene writer's system prompt. `fixed-style-blocks.txt` is the image prompt, with `{insert scene description here}` where the scene goes.

The live copies are `lib/reels/visual/scene-writer-prompt.txt` and `lib/reels/visual/fixed-style-blocks.txt`. To come back to this version, copy these two files over those.
