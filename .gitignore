# dependencies
node_modules/

# next.js
.next/
out/

# env — never commit real secrets
.env
.env.local
.env.*.local
scripts/gcp/worker.env
scripts/gcp/higgsfield-session.json
scripts/gcp/.deploy-env
SUPABASE.txt

# vercel
.vercel

# logs
*.log
npm-debug.log*

# os
.DS_Store
Thumbs.db
__MACOSX/

# editor
.vscode/
.idea/

# Local Python env for the reel text engine (Pillow + NumPy). Rebuilt on the VM.
helios_text_engine/.venv/
__pycache__/
*.pyc
tsconfig.tsbuildinfo
.orchestration-worker.lock
tmp/

# Local-only running bug ledgers / scratch (never commit)
local/

# Drafting PDF OCR scratch
resources/drafting/_pdf_images/

# Local-only bulk / tooling (not part of the dual-port deliverable)
pe-relationships-sf/
outreach brain/
eng.traineddata
.agents/
.claude/
.cursor/
__MACOSX/
skills-lock.json


# Large source headshots (use public/signatures/* instead)
Lucas Head Shot.JPG
Lucas Head Shot.jpg

# Keep always-on agent rules in git (rest of .cursor stays local)
!.cursor/
!.cursor/rules/
!.cursor/rules/**
